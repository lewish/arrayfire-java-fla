package arrayfire;

import arrayfire.datatypes.F32;
import arrayfire.numbers.N;
import arrayfire.numbers.U;

/**
 * Despite clearly breaking the Java class naming convention, this gives us a much shorter way to reference the
 * ArrayFire API that is more inline with what other frameworks do and makes for much more readable code.
 */
public class af extends ArrayFire {
}
